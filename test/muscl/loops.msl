
var counter = 0;

while (counter < 5) {
  send counter;
  counter += 1;
}

do {
  send counter;
  counter -= 1;
} while (counter > 0)

until (counter > 5) {
  send counter;
  counter += 1;
}

do {
  send counter;
  counter -= 1;
} until (counter < 1)

for var a from 1 to 10 {
  send a;
}
